service: tfm4-worker
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x

plugins:
  - serverless-offline
  - serverless-esbuild

custom:
  serverless-offline:
    httpPort: 4000

functions:
  api:
    handler: src/handler.handler
    events:
      - httpApi: '*'
