actions:
  - name: hello
    definition:
      kind: synchronous
      handler: '{{HASURA_ACTION_URL}}/reservation/create'
      request_transform:
        body:
          action: transform
          template: |-
            {
              "name": {{$body.input.name}}
            }
        template_engine: Kriti
        version: 2
custom_types:
  enums: []
  input_objects: []
  objects: []
  scalars: []
